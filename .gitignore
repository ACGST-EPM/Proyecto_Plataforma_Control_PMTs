# Artefactos temporales de Python (los genera cualquier verificación de sintaxis)
__pycache__/
*.py[cod]

# Sistema operativo
Thumbs.db
Desktop.ini
.DS_Store

# Temporales de edición
*~
*.tmp

# Dependencias de desarrollo del motor (se reinstalan con npm ci)
motor/node_modules/
# NOTA: motor/package-lock.json SI se versiona. Es lo que hace que `npm ci`
# instale exactamente las mismas versiones en cualquier equipo. Sin el, `npm ci`
# falla y las 3 pruebas que usan Turf no se pueden ejecutar: era la observacion
# pendiente de la auditoria de la Etapa 1.

# DATOS OPERATIVOS DE EPM: nunca deben subirse al repositorio publico
01_KMZ_Entrada/
*.kmz
reporte_dinamico.csv
crudo_*.json
